apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-service
spec:
  template:
    spec:
      containers:
        - name: book-service
          readinessProbe:
            initialDelaySeconds: 40
            periodSeconds: 3
          livenessProbe:
            initialDelaySeconds: 40
            periodSeconds: 3
